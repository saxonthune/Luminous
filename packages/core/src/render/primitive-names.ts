/** Names of the built-in render primitives. Plain data — safe to import headless. */
export const BUILTIN_PRIMITIVE_NAMES = [
  'text', 'vstack', 'hstack', 'card', 'badge', 'chip', 'icon',
  'divider', 'link', 'markdown', 'code-block', 'image', 'kv-list',
] as const;
