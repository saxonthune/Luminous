---
title: Vision
status: draft
summary: Luminous bridges human visual thinking and AI context — a canvas tool for software design that serves both
tags: [vision, visualization, canvas, software-design, ai-context]
deps: []
---

# Vision

## Core Principle

Humans do well with visual canvas tools. AI does well with high-quality structured context. These are complementary strengths, and the gap between them is where software design falls apart. Luminous exists to bridge this gap.

The canvas is an interface where humans can see, arrange, and reason about the artifacts of software — specifications, architecture, data models, concepts, and whatever other structures emerge from practice. At the same time, the structured data behind that canvas is context that AI agents can read, query, and act on. Work flows in both directions: a human sketches structure on the canvas, and an AI consumes it as precise context; an AI produces structured output, and a human sees it rendered spatially so they can evaluate and refine it.

This is what makes Luminous a *design* tool rather than a diagramming tool. Diagrams are pictures. Design is the activity of discovering the right structure, and that requires both human spatial intuition and AI's ability to hold large, precise context. Neither alone is sufficient.

## What Luminous Visualizes

The specific artifacts that belong on the canvas — and how they are best represented — will be decided within the context of practice, not prescribed upfront. The canvas engine, data model, and editor tools exist to serve this exploration. What matters is that whatever appears on the canvas is simultaneously legible to a human eye and consumable as structured context.

In practice, software design involves many kinds of structural model: state machines, component trees, resource dependency graphs, transformation pipelines, decision tables. These are all constrained graphs — nodes with types, edges with types, structural rules. Luminous doesn't prescribe which formalisms to use. It provides the substrate (canvas, typed nodes, typed edges) and lets users progressively add structure as their understanding deepens. Vocabulary first, types when patterns emerge, constraints when verification becomes valuable.

## The Name

The name comes from Christopher Alexander's *The Luminous Ground* — the quality of life in a built environment comes from the relationships between its parts, not from any single part alone. Software production has the same quality: the fit between product expectations and source code is determined by the relationships between all the artifacts in between.
