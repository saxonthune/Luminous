# Local-only canvas workspace — served by the file picker, never committed
gitignored/

# Pipeline-generated canvas artifacts (regenerated by scripts/analyze-*.ts)
*.canvas.json
# committed test fixtures
!rtp-statechart.canvas.json
!sample-primitives.canvas.json
